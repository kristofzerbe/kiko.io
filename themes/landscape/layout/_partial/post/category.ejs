<% if (post.categories && post.categories.length){ %>
  <div class="article-category category-<%= post.categories.data[0].name.toLowerCase() %>">
    <%- list_categories(post.categories, {
      show_count: false,
      class: 'article-category',
      style: 'none',
      separator: '►'
    }) %> 
  </div>
<% } %>