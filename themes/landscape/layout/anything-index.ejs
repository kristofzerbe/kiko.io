<h1 class="archive-title">
    <span style="font-weight: 600;"><%= page.title %></span>
</h1>

<% if (page.content){ %>
    <%- page.content %>
<% } %>

<% page.posts.each(function(item){ %>
    <%- partial('_partial/anything-item', { item: item, index: page.index }) %>
<% }) %>
